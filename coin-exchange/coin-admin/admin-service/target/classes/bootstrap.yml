spring:
  application:
    name: admin-service
    #开发环境配置
  profiles:
    active: dev  #dataID admin-service-dev.yaml
  cloud:
    nacos:
      server-addr: nacos-server:8848
      config:
        file-extension: yaml